apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - cluster.yaml
  - pooler.yaml
  # ScheduledBackup needs a configured object-store backup method first.
  # Add scheduled-backup.yaml only after completing the backup exercise.

