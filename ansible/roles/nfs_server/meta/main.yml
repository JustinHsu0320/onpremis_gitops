---
# =============================================================================
# roles/nfs_server/meta/main.yml
# =============================================================================
galaxy_info:
  role_name: nfs_server
  author: email_proxy_infra
  description: NFSv4.1-only file server for shared attachments and pgBackRest repo (nfs-01)
  license: MIT
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - all
  galaxy_tags:
    - nfs
    - storage

dependencies: []
